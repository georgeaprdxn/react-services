Footer

```jsx inside Markdown
import mockData from './__test__/Footer.mock.json'
;<Footer
	logo={mockData.logo}
	siteUrl={mockData.siteUrl}
	navigations={mockData.navigation}
	altText={mockData.altText}
	copyRightContent={mockData.copyRightContent}
/>
```
